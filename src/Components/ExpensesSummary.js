const ExpensesSumary = (props) => {


    
    
    // props.func('My name is Dean Winchester & this is my brother Sammie');

    return(
        <div className="ExpensesSumary">ExpensesSumary
        
        
        <h2>Expenses</h2>
      {/* <ul>
        {expenses.map((expense, index) => ( // enviar expenses como props
          <li key={index}>
            {expense.category} - {expense.product} - R$ {expense.price} - {expense.quantity} unidades - Selected Date: {expense.date}
          </li>
        ))}
      </ul> */}
        
        </div>
    )
}

export default ExpensesSumary

//////////////////////

